<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.startandroid.p0001androidstudy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".TestApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersonsListActivity"
            android:label="List of Persons"/>
        <activity android:name=".AddressListActivity"
            android:label="List of Addresses"/>
        <meta-data android:name="ru.startandroid.p0001androidstudy.glide.AppGlideModule"
            android:value="GlideModule"/>
    </application>

</manifest>
